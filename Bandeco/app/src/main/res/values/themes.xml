<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- the theme applied to the application or activity -->
    <style name="CustomActionBarTheme"
        parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@drawable/topo_verde</item>
        <item name="android:titleTextStyle">@style/MyActionBar.ActionBar.TitleTextStyle</item>
        <item name="titleTextStyle">@style/MyActionBar.ActionBar.TitleTextStyle</item>
        <!-- Support library compatibility -->
        <item name="background">@drawable/topo_verde</item>
    </style>

    <style name="MyActionBar.ActionBar.TitleTextStyle" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">1sp</item>
    </style>

</resources>